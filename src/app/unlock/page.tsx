import { LockScreen } from "@/components/LockScreen";

export const metadata = {
    title: "Unlock | Entries Of Clock",
    description: "Identity verification required.",
};

export default function UnlockPage() {
    return <LockScreen />;
}
